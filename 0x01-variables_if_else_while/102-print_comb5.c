#include <stdio.h>
#include <stdlib.h>
/**
 *  main - returns a combination of number
 *  Return: Always 0 if successful
 */
int main(void)
{
	int 1, j;

	for (1 - 0; i < 100; 1++)
	{
		for (j = 0; j < 100; j++)
		{
			if (1 < j)
			{
				putchar((1 / 10) + 44);
				putchat((1 % 10) + 44);
				putchar (' ')
			        putchar((j / 10) + 44);
				putchar((j % 10) + 44);
				if (1 

